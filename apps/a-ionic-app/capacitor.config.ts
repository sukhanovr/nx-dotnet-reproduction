import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'a-ionic-app',
  webDir: '../../dist/apps/a-ionic-app',
  bundledWebRuntime: false,
};

export default config;
